{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check if this bash command is a git commit command (commands containing 'git commit'). If it is a git commit, respond with: {\"decision\": \"approve\", \"systemMessage\": \"Please run the /document-codebase command to update documentation before committing.\"}. Otherwise, respond with: {\"decision\": \"approve\"}. The command is: $ARGUMENTS",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
